/*
import { test } from "tape";

let failed = false;

test.onFinish(() => {
    if (failed) {
        console.error('Some tests failed. Please check the test results for details.');
        process.exit(-1);
    }
    console.log('All tests have completed.');
    process.exit(0);
});

test.onFailure(() => {
    failed = true;
})
*/

export {}
